# 复制本文件为 .env 并填入真实值（.env 已被 gitignore，不会进仓库）
# 获取方式：
#   - CLOUDFLARE_API_TOKEN: dash.cloudflare.com → 右上角头像 → My Profile → API Tokens → Create Token
#     （用 "Edit Cloudflare Workers" 模板即可，含 workers_scripts / workers_routes / workers_kv 权限）
#   - CLOUDFLARE_ACCOUNT_ID: dash.cloudflare.com 首页右侧 Account ID（或 Workers 页面 URL 中）
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# 可选：Tavily 搜索 API key（推荐配置，作为默认链的可靠基座）
# 不配置时默认链自动退化为 baidu+so360
# 获取：https://app.tavily.com 注册，免费 1000 次/月
# 更安全的方式：用 wrangler secret put TAVILY_API_KEY，而不是写在 .env
TAVILY_API_KEY=
